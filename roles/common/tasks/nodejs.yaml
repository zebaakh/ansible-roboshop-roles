- name: setup NodeSource repository for LTS Node.js
  ansible.builtin.shell: "curl -sL https://rpm.nodesource.com/setup_lts.x | bash"

- name: Install Node.js
  ansible.builtin.yum:
    name: nodejs
    state: installed